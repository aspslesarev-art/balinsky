// Текст «О комплексе» на странице ЖК.
//
// Раньше текст собирался из четырёх источников с приоритетом
// `assistant_kb.page_body` → `SEO Text` → `Описание` → `ИИ Описание 2`, и
// первый (сгенерированный) перекрывал всё остальное. Значит, отредактировать
// видимый текст из админки было нельзя: правка `ИИ Описание 2` до страницы не
// долетала.
//
// Это поле — ручное переопределение с наивысшим приоритетом: непустое
// значение показывается как есть, пустое означает «оставить сгенерированный
// текст». Поэтому оно исключено из авто-заполнения ИИ (`noAi` в
// lib/admin/collections.ts) — иначе первый же сейв записи подменил бы
// выверенный KB-текст свежесгенерированным.
//
// Источник правды — русский текст, как и у инлайн-редактирования на странице.
// Остальные языки продолжают брать свой перевод из KB.
export const COMPLEX_DESCRIPTION_FIELD = 'Описание ЖК'
